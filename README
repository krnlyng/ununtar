Another command line tool you didn't know that you needed. Removes files
created by an (accidental) untar/unzip/unrar operation. Also tries to reinstall
packages if files of packages have been overwritten, supports pacman, apt-get,
zypper and yum.

# EXAMPLES:

help:
$ ununtar -h

remove files generated by a tar extract operation:
$ ununtar mytar.tar.gz

no warnings:
$ ununtar -s mytar.tar.gz

zip or rar file:
$ ununtar myzip.zip
$ ununtar myrar.rar

specify directory on which to operate:
$ ununtar -d / mytar.tar.gz

specify package manager (by default tries to figure out by itself):
$ ununtar -p apt-get -d / mytar.tar.gz

keep empty directories:
$ ununtar -k mytar.tar.gz

don't try to reinstall packages:
$ ununtar -nrp -d / mytar.tar.gz

